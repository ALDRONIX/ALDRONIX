<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>OSINT WHATSAPP - ALDRONIX</title>
    <style>
    /* Info Card - Warna Biru Cerah */
    /* Info Card - Warna #15dceb */
.info-card .info-row {
    color: #15dceb;
}
.info-card .info-value {
    color: #15dceb !important;
    font-weight: bold;
    text-shadow: 1px 1px 0 #000000;
}
.info-card .info-label {
    color: #15dceb;
    font-weight: bold;
}
.info-card h3 {
    background: #000;
    color: #05fbf7;
    display: inline-block;
    padding: 4px 10px;
    font-size: 11px;
    margin-bottom: 12px;
    font-family: 'Roboto Mono', monospace;
    letter-spacing: 1px;
}
.info-row {
    display: flex;
    justify-content: space-between;
    padding: 6px 0;
    border-bottom: 1px solid #00000033;
    font-size: 11px;
    font-family: 'Roboto Mono', monospace;
}
.info-label {
    font-weight: bold;
    color: #000;
}
.info-value {
    color: #05fbf7;
    font-weight: bold;
    text-shadow: 0 0 2px #000;
}
        @import url('https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@700&family=Roboto+Mono:wght@400;700&display=swap');

        * { margin: 0; padding: 0; box-sizing: border-box; }

        .top-bar {
            position: fixed; top: 0; left: 0; width: 100%;
            background: #000; color: #05fbf7; font-family: 'Roboto Mono', monospace;
            font-size: 13px; font-weight: bold; padding: 10px 0;
            border-bottom: 2px solid #000; z-index: 1000;
            overflow: hidden; white-space: nowrap;
        }
        .top-bar marquee { width: 100%; display: block; }

        body {
            background-color: #ffffff;
            background-image: radial-gradient(#000 0.8px, transparent 0.8px);
            background-size: 15px 15px;
            font-family: 'Space Grotesk', sans-serif;
            display: flex;
            justify-content: center; align-items: center;
            min-height: 100vh; padding: 80px 20px 20px 20px;
        }

        .main-card { width: 100%; max-width: 380px; display: flex; flex-direction: column; gap: 15px; position: relative; }

        .header-card {
            background: #05fbf7; border: 3px solid #000;
            box-shadow: 10px 10px 0px #000; padding: 20px; position: relative;
            
            
        }
        body {
    background-image: url('ALDRONIX.BG.jpeg');
    background-size: cover;
    background-position: center;
    background-attachment: fixed;
    background-repeat: no-repeat;
    font-family: 'Space Grotesk', sans-serif;
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 100vh;
    padding: 80px 20px 20px 20px;
}
        .logo-img {
            width: 80px; height: 80px; background: #ffffff;
            border: 3px solid #000; box-shadow: 4px 4px 0px #000;
            margin-bottom: 15px; display: flex; align-items: center; justify-content: center; font-size: 12px; font-weight: bold;
        }
        body {
    background-image: url('ALDRONIX.BG.jpeg');
    background-size: cover;
    background-position: center;
    background-attachment: fixed;
    background-repeat: no-repeat;
    font-family: 'Space Grotesk', sans-serif;
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 100vh;
    padding: 80px 20px 20px 20px;
}
        .nav-btns {
            position: absolute; top: 15px; right: 15px;
            display: flex; flex-direction: column; gap: 5px;
        }
        .btn-s {
            background: #15dceb; border: 2px solid #000; box-shadow: 3px 3px 0px #000;
            padding: 4px 8px; font-size: 10px; font-weight: 700; text-decoration: none; color: #000; cursor: pointer;
        }
        .header-card h2 { color: #000000; font-size: 12px; margin-bottom: 4px; text-transform: uppercase; }
        .header-card h1 { color: #000000; font-size: 22px; line-height: 1; text-transform: uppercase; }

        .form-card {
            background: #fff; border: 3px solid #000;
            box-shadow: 10px 10px 0px #000; padding: 20px;
        }
        .input-box { margin-bottom: 15px; }
        .input-box label { display: block; font-size: 11px; font-weight: bold; margin-bottom: 5px; text-transform: uppercase; }
        input {
            width: 100%; padding: 12px; border: 3px solid #000;
            font-family: 'Roboto Mono', monospace; font-size: 14px; outline: none;
        }

        .custom-select-wrapper { position: relative; cursor: pointer; }
        .select-trigger {
            width: 100%; padding: 12px; border: 3px solid #000;
            background: #fff; font-family: 'Roboto Mono', monospace; font-size: 14px;
            display: flex; justify-content: space-between; align-items: center;
        }
        .custom-options {
            position: absolute; top: 100%; left: -3px; right: -3px;
            background: #fff; border: 3px solid #000; z-index: 100;
            display: none; box-shadow: 8px 8px 0px #000;
        }
        .custom-options.open { display: block; }
        .option { padding: 10px; border-bottom: 2px solid #000; font-family: 'Roboto Mono', monospace; font-size: 12px; }
        .option:hover { background: #15dceb; }
        .option:last-child { border-bottom: none; }

        .btn-go {
            width: 100%; background: #15dceb; border: 3px solid #000;
            box-shadow: 6px 6px 0px #000; padding: 15px;
            font-size: 16px; font-weight: bold; text-transform: uppercase; cursor: pointer; margin-top: 10px;
        }
        .btn-go:active { box-shadow: 0px 0px 0px #000; transform: translate(6px, 6px); }

        #customAlert {
            display: none; position: absolute; top: 50%; left: 50%; 
            transform: translate(-50%, -50%); width: 90%;
            background: #15dceb; border: 4px solid #000; 
            box-shadow: 10px 10px 0px #000; padding: 20px;
            z-index: 3000; color: #fff; text-align: center;
            animation: shake 0.3s ease-in-out;
        }
        @keyframes shake {
            0% { transform: translate(-52%, -50%); }
            50% { transform: translate(-48%, -50%); }
            100% { transform: translate(-50%, -50%); }
        }

        .modal {
            display: none; position: fixed; top: 0; left: 0; width: 100%; height: 100%;
            background: rgba(0,0,0,0.9); z-index: 2000; justify-content: center; align-items: center; padding: 20px;
        }
        .modal-content {
            background: #fff; border: 4px solid #000; box-shadow: 12px 12px 0px #15dceb;
            width: 100%; max-width: 350px; padding: 20px; max-height: 85vh; overflow-y: auto;
        }
        .modal-content h3 { border-bottom: 3px solid #000; margin-bottom: 10px; padding-bottom: 5px; text-transform: uppercase; }
        .modal-content p { font-size: 13px; font-family: 'Roboto Mono', monospace; margin-bottom: 10px; line-height: 1.4; color: #333; }
        
        .result-box {
            background: #000; color: #03ff35; padding: 15px; font-family: 'Roboto Mono', monospace;
            font-size: 11px; border: 2px solid #15dceb; margin-top: 10px; display: none; white-space: pre-wrap;
        }

        .tag-input-area {
            margin-top: 10px; border-top: 2px dashed #000; padding-top: 10px;
            display: none;
        }
        .tag-input-area input { border: 2px solid #000; padding: 5px; font-size: 12px; margin-bottom: 5px; width: 100%; }
        .btn-save-tag { background: #000; color: #17c5d1; border: none; padding: 8px; cursor: pointer; font-size: 11px; width: 100%; font-weight: bold; text-transform: uppercase; }

        .btn-close { width: 100%; background: #17c5d1; border: 2px solid #000; color: #000000; padding: 8px; font-weight: bold; cursor: pointer; margin-top: 10px; }

        .scan-line { height: 4px; background: #17c5d1; position: relative; overflow: hidden; display: none; margin-bottom: 10px; border: 1px solid #000; }
        .scan-line::after { content: ''; display: block; width: 40%; height: 100%; background: #000; position: absolute; animation: scan 1.5s linear infinite; }
        @keyframes scan { 0% { left: -40%; } 100% { left: 100%; } }
    </style>
</head>
<body>

<div class="top-bar">
    <marquee direction="left" scrollamount="7">
        AUTHORIZED ACCESS ONLY — RESALE, DUPLICATION, OR UNAUTHORIZED DISTRIBUTION IS PROHIBITED. - ALDRONIX
    </marquee>
</div>

<div class="main-card">
   <div class="header-card">
    <div class="nav-btns">
        <div class="btn-s" onclick="openModal('infoModal')">SYSTEM INFORMATION</div>
        <div class="btn-s" onclick="openModal('joinModal')" style="background:#fff">NETWORK ACCESS</div>
    </div>

<!-- JAM REAL TIME -->
<div style="position: absolute; top: 78px; right: 10px; background: #000; border: 2px solid #15dceb; padding: 5px 12px; font-family: monospace; font-size: 12px; font-weight: bold; color: #15dceb; text-align: center; box-shadow: 3px 3px 0px #000; z-index: 999;">
    <span id="realTimeClock">--:--:--</span>
</div>

<!-- TOMBOL MUSIK -->
<div id="musicBtn" style="position: absolute; top: 80px; right: 110px; background: #000; border: 2px solid #15dceb; padding: 4px 8px; cursor: pointer; z-index: 999; font-family: monospace; font-size: 10px; color: #15dceb; text-align: center; box-shadow: 2px 2px 0px #000;" onclick="toggleMusic()">
    🎵 PLAY
</div>

    <!-- ========== LOGO LINGKARAN (TENGAH ATAS) ========== -->
<!-- ========== LOGO LINGKARAN (RADA KE KIRI) ========== -->
<div style="margin-bottom: 15px;">
    <div style="width: 85px; height: 85px; border-radius: 50%; border: 3px solid #000; background: #fff; overflow: hidden; box-shadow: 4px 4px 0px #000;">
        <img src="ALDRONIX.PPV3.jpeg" alt="Logo" style="width: 100%; height: 100%; object-fit: cover;">
    </div>
</div>
<!-- ========== END LOGO ========== -->

    <h2>TOOLS by ALDRONIX</h2>
    <h1>WA ANALYTICS SYSTEM<br>V2.0 [Exclusive Edition]</h1>
</div>

    <div class="form-card">
        <div class="input-box">
            <label>PHONE NUMBER IDENTIFIER:</label>
            <input type="text" id="phone" placeholder="628xxxxxxx atau email@domain.com">
        </div>

        <div class="input-box">
            <label>OSINT OPERATION PANEL:</label>
            <div class="custom-select-wrapper">
                <div class="select-trigger" id="selectTrigger" onclick="toggleSelect()">
                    <span id="selectedLabel">-- SELECT OSINT MODE --</span>
                    <span>▼</span>
                </div>
                <div class="custom-options" id="optionsBox">
                    <div class="option" onclick="selectOption('CEK OSINT', 'provider')">OSINT ANALYSIS</div>
                    <div class="option" onclick="selectOption('TRACKING ONLINE', 'tracking')">LIVE ACTIVITY</div>
                    <div class="option" onclick="selectOption('GETCONTACT DATABASE', 'getcontact')">CONTACT DATABASE</div>
                    <div class="option" onclick="selectOption('STATUS WHATSAPP', 'status')">ACCOUNT STATUS</div>
                    <div class="option" onclick="selectOption('CEK EMAIL', 'email')">CEK EMAIL [Premium Tools]</div>
                </div>
            </div>
            <input type="hidden" id="mode">
        </div>

        <button class="btn-go" onclick="startTracking()">START ANALYSIS</button>
    </div>

    <div id="customAlert">
        <h4 style="margin-bottom: 10px;">⚠️ ACCESS DENIED!</h4>
        <p id="alertText" style="font-size: 12px; margin-bottom: 15px;">MASUKKAN DATA SECARA LENGKAP!</p>
        <button onclick="closeAlert()" style="background:#000; color:#fff; border:none; padding:5px 15px; cursor:pointer; font-weight:bold;">RETRY</button>

</div>

<!-- ========== FOOTER (PAS DI SINI, SEBELUM MODAL) ========== -->
<div class="footer" style="color: #00fff7; text-shadow: 0 0 5px #000000, 0 0 10px #000000; font-weight: bold; background: rgba(0,0,0,0.5); padding: 8px; border-radius: 20px; text-align: center; margin-top: 15px;">
    <p>- POWERED BY ALDRONIX -</p>
</div>
<!-- ========== END FOOTER ========== -->

<!-- Modal Info Baca -->
<div id="infoModal" class="modal">
    <div class="modal-content">
        <h3>APA ITU OSINT?</h3>
        <p><strong>OSINT (Open Source Intelligence)</strong> adalah metode pengumpulan intelijen melalui data primer yang tersedia secara publik di internet.</p>
        <p>Pada Tools ini, sistem melakukan sinkronisasi dengan database <strong>HLR (Home Location Register)</strong> untuk menentukan titik registrasi awal sebuah nomor telepon (MSISDN).</p>
        <p><strong>Fungsi Utama:</strong><br>
        1. <strong>Tracking Lokasi:</strong> Mendeteksi wilayah Provinsi dan Kabupaten berdasarkan prefix operator.<br>
        2. <strong>Identity Check:</strong> Menarik metadata Getcontact untuk melihat reputasi nomor (Penipu/Bukan).<br>
        3. <strong>WA Metadata:</strong> Verifikasi keaktifan akun dan tipe platform yang digunakan (Business/Regular).</p>
        <button class="btn-close" onclick="closeModal('infoModal')">MENGERTI</button>
    </div>
</div>
<!-- ========== SISTEM INFORMASI PERANGKAT ========== -->
<div class="info-card">
    <h3>🖥️ DEVICE FORENSIC DATA</h3>
    <div class="info-row">
        <span class="info-label">📱 TIPE PERANGKAT:</span>
        <span class="info-value" id="deviceType">-</span>
    </div>
    <div class="info-row">
        <span class="info-label">🏷️ MERK/BRAND:</span>
        <span class="info-value" id="deviceBrand">-</span>
    </div>
    <div class="info-row">
        <span class="info-label">🔋 BATERAI:</span>
        <span class="info-value" id="batteryLevel">-</span>
    </div>
    <div class="info-row">
        <span class="info-label">🆓 SESSION ID:</span>
        <span class="info-value" id="sessionId">-</span>
    </div>
    <div class="info-row">
        <span class="info-label">🌐 USER AGENT:</span>
        <span class="info-value" id="userAgent" style="font-size: 8px; word-break: break-all;">-</span>
    </div>
</div>
<!-- ========== END INFO PERANGKAT ========== -->

<!-- Modal Join -->
<div id="joinModal" class="modal">
    <div class="modal-content">
        <h3>AKSES CEPAT</h3>
        <a href="https://whatsapp.com/channel/0029VbCeMP7Bfxo2uP6FC43T" class="modal-link" target="_blank" style="display: block; background: #000; color: #ccff00; padding: 10px; text-align: center; text-decoration: none; font-weight: bold; margin-bottom: 8px; border: 2px solid #ccff00;">SALURAN WHATSAPP</a>
        <a href="https://www.tiktok.com/@aldy.delapannn?_r=1&_t=ZS-96NVPJCQ1FI" class="modal-link" target="_blank" style="display: block; background: #000; color: #ccff00; padding: 10px; text-align: center; text-decoration: none; font-weight: bold; margin-bottom: 8px; border: 2px solid #ccff00;">TIKTOK RESMI</a>
        <a href="https://aldronix.github.io/ALDRONIX/Aldronix.cyber.html" class="modal-link" target="_blank" style="display: block; background: #000; color: #ccff00; padding: 10px; text-align: center; text-decoration: none; font-weight: bold; margin-bottom: 8px; border: 2px solid #ccff00;">WEBSITE</a>
        <button class="btn-close" onclick="closeModal('joinModal')">CLOSE</button>
    </div>
</div>

<div id="resultModal" class="modal">
    <div class="modal-content">
        <h3>OSINT RESULT</h3>
        <p id="trackingStatus">Connecting...</p>
        <div class="scan-line" id="loader"></div>
        <div class="result-box" id="resultData"></div>
        
        <div id="tagInputArea" class="tag-input-area">
            <p style="font-size: 11px; font-weight: bold; margin-bottom: 5px;">TAMBAHKAN TAG BARU UNTUK NOMOR INI:</p>
            <input type="text" id="newTagName" placeholder="Contoh: Penipu Paket / Teman Sekolah">
            <button class="btn-save-tag" onclick="saveNewTag()">SIMPAN KE DATABASE</button>
        </div>

        <button class="btn-close" id="btnCloseResult" onclick="closeModal('resultModal')" style="display:none">DASHBOARD</button>
    </div>
</div>


<script>
  // ========== CEK BREACH EMAIL (REAL API) ==========
async function checkBreach(email) {
    try {
        const response = await fetch(`https://leakcheck.net/api/public?check=${email}`);
        if (response.ok) {
            const data = await response.json();
            return data;
        }
    } catch(e) {
        console.log("API error, pakai simulasi");
    }
    return null;
}
  function isValidEmail(email) {
    const regex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
    return regex.test(email);
}
  // ========== DEVICE FORENSIC DATA ==========
function getDeviceInfo() {
    // Tipe Perangkat
    const ua = navigator.userAgent;
    let deviceType = "Desktop / Laptop";
    if (/Mobi|Android|iPhone|iPad|iPod/i.test(ua)) {
        deviceType = "Mobile / Tablet";
    }
    const deviceTypeEl = document.getElementById('deviceType');
    if (deviceTypeEl) deviceTypeEl.innerText = deviceType;

    // Merk/Brand (deteksi lengkap)
    let brand = "Unknown";
    if (ua.includes("iPhone")) brand = "Apple";
    else if (ua.includes("Samsung")) brand = "Samsung";
    else if (ua.includes("Xiaomi")) brand = "Xiaomi";
    else if (ua.includes("Redmi")) brand = "Xiaomi Redmi";
    else if (ua.includes("POCO")) brand = "Xiaomi POCO";
    else if (ua.includes("OPPO")) brand = "OPPO";
    else if (ua.includes("vivo")) brand = "vivo";
    else if (ua.includes("Realme")) brand = "Realme";
    else if (ua.includes("OnePlus")) brand = "OnePlus";
    else if (ua.includes("Nokia")) brand = "Nokia";
    else if (ua.includes("Motorola")) brand = "Motorola";
    else if (ua.includes("Google") && ua.includes("Pixel")) brand = "Google Pixel";
    else if (ua.includes("ASUS")) brand = "ASUS";
    else if (ua.includes("Sony")) brand = "Sony";
    else if (ua.includes("Huawei")) brand = "Huawei";
    else if (ua.includes("Honor")) brand = "Honor";
    else if (ua.includes("Nothing")) brand = "Nothing Phone";
    else if (ua.includes("TECNO")) brand = "TECNO";
    else if (ua.includes("itel")) brand = "itel";
    else if (ua.includes("Infinix")) brand = "Infinix";
    else if (ua.includes("Lava")) brand = "Lava";
    else if (ua.includes("Sharp")) brand = "Sharp";
    else if (ua.includes("Microsoft")) brand = "Microsoft";
    else if (ua.includes("Windows")) brand = "Windows PC";
    else if (ua.includes("Mac")) brand = "Apple Mac";
    else if (ua.includes("Linux") && !ua.includes("Android")) brand = "Linux PC";
    
    // Deteksi dari model Android (jika masih unknown)
    if (brand === "Unknown" && ua.includes("Android")) {
        const modelMatch = ua.match(/Android [^;]+; ([^)]+)/);
        if (modelMatch && modelMatch[1]) {
            let model = modelMatch[1];
            if (model.includes("TECNO")) brand = "TECNO";
            else if (model.includes("itel")) brand = "itel";
            else if (model.includes("Infinix")) brand = "Infinix";
            else if (model.includes("Redmi")) brand = "Xiaomi Redmi";
            else brand = model.split(" ")[0];
        }
    }
    
    const brandEl = document.getElementById('deviceBrand');
    if (brandEl) brandEl.innerText = brand;

    // Baterai
    if ('getBattery' in navigator) {
        navigator.getBattery().then(function(battery) {
            let level = Math.floor(battery.level * 100);
            let status = battery.charging ? "🔌 (Mengisi)" : "🔋";
            const batteryEl = document.getElementById('batteryLevel');
            if (batteryEl) batteryEl.innerText = `${level}% ${status}`;
        }).catch(function() {
            const batteryEl = document.getElementById('batteryLevel');
            if (batteryEl) batteryEl.innerText = "Tidak tersedia";
        });
    } else {
        const batteryEl = document.getElementById('batteryLevel');
        if (batteryEl) batteryEl.innerText = "Tidak didukung";
    }

    // Session ID (acak)
    const sessionId = Math.random().toString(36).substring(2, 12).toUpperCase();
    const sessionEl = document.getElementById('sessionId');
    if (sessionEl) sessionEl.innerText = sessionId;

    // User Agent (dipotong)
    let shortUA = ua.length > 55 ? ua.substring(0, 55) + "..." : ua;
    const uaEl = document.getElementById('userAgent');
    if (uaEl) uaEl.innerText = shortUA;
}

// Jalankan saat halaman selesai loading
getDeviceInfo();
  // ========== JAM REAL TIME ==========
function updateClock() {
    const now = new Date();
    const hours = String(now.getHours()).padStart(2, '0');
    const minutes = String(now.getMinutes()).padStart(2, '0');
    const seconds = String(now.getSeconds()).padStart(2, '0');
    const timeString = `${hours}:${minutes}:${seconds}`;
    const clockElement = document.getElementById('realTimeClock');
    if (clockElement) {
        clockElement.innerText = timeString;
    }
}
updateClock();
setInterval(updateClock, 1000);
    function openModal(id) { document.getElementById(id).style.display = 'flex'; }
    function closeModal(id) { document.getElementById(id).style.display = 'none'; }
    function toggleSelect() { document.getElementById('optionsBox').classList.toggle('open'); }
    function selectOption(label, value) {
        document.getElementById('selectedLabel').innerText = label;
        document.getElementById('mode').value = value;
        toggleSelect();
    }
    function showAlert(msg) { document.getElementById('alertText').innerText = msg; document.getElementById('customAlert').style.display = 'block'; }
    function closeAlert() { document.getElementById('customAlert').style.display = 'none'; }

    function getIndonesiaProvider(num) {
        let p = { name: "UNKNOWN OPERATOR", prov: "UNKNOWN REGION", kab: "UNKNOWN AREA" };
        if (/^628(11)/.test(num)) { p.name = "TELKOMSEL HALO (PASCA)"; p.prov = "DKI JAKARTA"; p.kab = "JAKARTA PUSAT"; }
        else if (/^628(12|13|21|22)/.test(num)) { p.name = "TELKOMSEL SIMPATI/AS"; p.prov = "JAWA BARAT"; p.kab = "BANDUNG / KOTA"; }
        else if (/^628(52|53|23)/.test(num)) { p.name = "TELKOMSEL KARTU AS"; p.prov = "JAWA TENGAH"; p.kab = "KEBUMEN / PURWOKERTO"; }
        else if (/^628(51)/.test(num)) { p.name = "TELKOMSEL BY.U / AS"; p.prov = "JAWA TIMUR"; p.kab = "MALANG / SURABAYA"; }
        else if (/^628(57|58)/.test(num)) { p.name = "INDOSAT IM3"; p.prov = "JAWA TIMUR"; p.kab = "MALANG / SURABAYA"; }
        else if (/^628(14|15|16|55|56)/.test(num)) { p.name = "INDOSAT MENTARI/MATRIX"; p.prov = "DKI JAKARTA"; p.kab = "JAKARTA BARAT"; }
        else if (/^628(17|18|19|59|77|78)/.test(num)) { p.name = "XL AXIATA / LIVE.ON"; p.prov = "BANTEN"; p.kab = "TANGERANG / SERANG"; }
        else if (/^628(31|38)/.test(num)) { p.name = "AXIS (XL AXIATA)"; p.prov = "JAWA BARAT"; p.kab = "BOGOR / BEKASI"; }
        else if (/^628(95|96|97|98|99)/.test(num)) { p.name = "HUTCHISON THREE (3)"; p.prov = "SUMATERA UTARA"; p.kab = "MEDAN / DELI SERDANG"; }
        else if (/^628(81|82|83|84|85|86|87|88|89)/.test(num)) { p.name = "SMARTFREN"; p.prov = "JAWA TENGAH"; p.kab = "SEMARANG / SOLO"; }
        return p;
    }

    function getStoredTags(phone) {
        let db = JSON.parse(localStorage.getItem('aldronix_osint_db') || '{}');
        return db[phone] || ["Belum Ada Tag (Database Kosong)"];
    }

    function saveNewTag() {
        const phone = document.getElementById('phone').value;
        const newTag = document.getElementById('newTagName').value;
        if(!newTag) return;
        let db = JSON.parse(localStorage.getItem('rapzz_osint_db') || '{}');
        if(!db[phone]) db[phone] = [];
        db[phone].push(newTag);
        localStorage.setItem('aldronix_osint_db', JSON.stringify(db));
        document.getElementById('newTagName').value = "";
        alert("TAG BARU TELAH DISIMPAN KE DATABASE!");
        startTracking();
    }

// ========== VALIDASI EMAIL ==========
function isValidEmail(email) {
    const regex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
    return regex.test(email);
}

// ========== CEK EMAIL (LENGKAP & PROFESIONAL) ==========
function cekEmail(email) {
    let result = `═══════════════════════════════════════\n`;
    result += `📧 HASIL OSINT EMAIL\n`;
    result += `═══════════════════════════════════════\n\n`;
    
    result += `📧 EMAIL: ${email}\n`;
    
    // Deteksi provider email
    let domain = email.split('@')[1].toLowerCase();
    let provider = "Unknown";
    let logo = "📧";
    if (domain.includes("gmail")) { provider = "Google (Gmail)"; logo = "🔴"; }
    else if (domain.includes("yahoo")) { provider = "Yahoo"; logo = "🟣"; }
    else if (domain.includes("outlook") || domain.includes("hotmail")) { provider = "Microsoft"; logo = "🔵"; }
    else if (domain.includes("proton")) { provider = "ProtonMail"; logo = "🟢"; }
    else if (domain.includes("icloud") || domain.includes("me.com")) { provider = "Apple"; logo = "⚪"; }
    else provider = "Custom Domain";
    
    result += `${logo} PROVIDER: ${provider}\n`;
    result += `📏 PANJANG EMAIL: ${email.length} karakter\n`;
    
    // Cek format email (plus alias)
    if (email.includes("+")) {
        result += `🏷️ PLUS ALIAS: Terdeteksi (biasa dipakai untuk filtering)\n`;
    }
    
    // Deteksi username
    let username = email.split('@')[0];
    result += `👤 USERNAME: ${username}\n`;
    
    // Estimasi usia akun (simulasi)
    const year = 2020 + Math.floor(Math.random() * 5);
    result += `📅 ESTIMASI DIBUAT: Tahun ${year} (simulasi)\n`;
    
    // Cek domain publik atau custom
    result += `🌐 DOMAIN: ${domain.includes("gmail") || domain.includes("yahoo") || domain.includes("outlook") ? "Publik (Umum)" : "Private/Custom"}\n`;
    
    // Simulasi status terdaftar di platform
    const platforms = ["WhatsApp", "Telegram", "Instagram", "GitHub", "Twitter/X"];
    let registered = [];
    for (let i = 0; i < platforms.length; i++) {
        if (Math.random() > 0.5) registered.push(platforms[i]);
    }
    
    result += `\n═══════════════════════════════════════\n`;
    result += `📱 TERDAFTAR DI PLATFORM:\n`;
    if (registered.length > 0) {
        registered.forEach(p => { result += `   ✅ ${p}\n`; });
    } else {
        result += `   ❌ Tidak terdeteksi\n`;
    }
    
    result += `\n═══════════════════════════════════════\n`;
    result += `🔒 STATUS KEAMANAN:\n`;
    result += `   ${Math.random() > 0.7 ? "⚠️ Pernah terdeteksi di breach database (simulasi)" : "✅ Aman, tidak terdeteksi di breach database"}\n`;
    
    result += `\n═══════════════════════════════════════\n`;
    result += `⚠️ Catatan: Data ini hanya simulasi untuk demo.\n`;
    result += `═══════════════════════════════════════\n`;
    
    return result;
}

    function startTracking() {
        const phone = document.getElementById('phone').value;
        const mode = document.getElementById('mode').value;
        if (!phone || !mode) { showAlert("PILIH OPSI DAN ISI NOMOR!"); return; }

// Validasi khusus: hanya untuk mode nomor WA (bukan email)
if (mode !== 'email') {
    if (!phone.startsWith("62")) { 
        showAlert("GUNAKAN FORMAT 628XXXX!"); 
        return; 
    }
}

        openModal('resultModal');
        const status = document.getElementById('trackingStatus');
        const loader = document.getElementById('loader');
        const result = document.getElementById('resultData');
        const tagArea = document.getElementById('tagInputArea');
        const closeBtn = document.getElementById('btnCloseResult');

        status.innerText = "BYPASSING FIREWALL...";
        loader.style.display = 'block';
        result.style.display = 'none';
        tagArea.style.display = 'none';
        closeBtn.style.display = 'none';

        setTimeout(() => {
            status.innerText = "EXTRACTION SUCCESS!";
            loader.style.display = 'none';
            result.style.display = 'block';
            closeBtn.style.display = 'block';

            const info = getIndonesiaProvider(phone);
            const randomID = Math.floor(1000 + Math.random() * 9000);
            const date = new Date();
            const timeString = date.toLocaleString('id-ID');
            
            let log = `[ALDRONIX_OSINT_REPORT_${randomID}]\n`;
            log += `TARGET : +${phone}\n`;
            log += `--------------------------\n`;

            if(mode === 'provider') {
                log += `KARTU    : ${info.name}\n`;
                log += `PROVINSI : ${info.prov}\n`;
                log += `KABUPATEN: ${info.kab}\n`;
                log += `SIGNAL   : 4G/LTE (CELL_ACTIVE)\n`;
                log += `HLR_CODE : ${phone.substring(0,6)}\n`;
                log += `STATUS   : VERIFIED (INDONESIA)\n`;
                log += `--------------------------\n`;
                log += `NOTE: GENERATED DATA IS BASED ON PUBLIC PREFIX & REGISTRATION ANALYSIS — NOT REALTIME TARGET LOCATION.`;
            } 
            else if(mode === 'tracking') {
                log += `IP_ADDR  : 182.${Math.floor(Math.random()*255)}.${Math.floor(Math.random()*255)}.${Math.floor(Math.random()*255)}\n`;
                log += `LAST_ON  : ${timeString}\n`;
                log += `LOC_ACT  : ${info.kab}, ${info.prov}\n`;
                log += `ACTIVITY : CHATTING / IDLE\n`;
                log += `ACCURACY : 85% (CELLULAR DATA)`;
            }
            else if(mode === 'email') {
    const email = phone;
    if (!isValidEmail(email)) {
        log = `❌ FORMAT EMAIL TIDAK VALID!\nContoh: nama@domain.com`;
    } else {
        let domain = email.split('@')[1].toLowerCase();
        let provider = "Unknown";
        let logo = "📧";
        if (domain.includes("gmail")) { provider = "Google (Gmail)"; logo = "🔴"; }
        else if (domain.includes("yahoo")) { provider = "Yahoo"; logo = "🟣"; }
        else if (domain.includes("outlook") || domain.includes("hotmail")) { provider = "Microsoft"; logo = "🔵"; }
        else if (domain.includes("proton")) { provider = "ProtonMail"; logo = "🟢"; }
        else if (domain.includes("icloud") || domain.includes("me.com")) { provider = "Apple"; logo = "⚪"; }
        else provider = "Custom Domain";
        
        let username = email.split('@')[0];
        const year = 2020 + Math.floor(Math.random() * 5);
        const platforms = ["WhatsApp", "Telegram", "Instagram", "GitHub", "Twitter/X"];
        let registered = [];
        for (let i = 0; i < platforms.length; i++) {
            if (Math.random() > 0.5) registered.push(platforms[i]);
        }
        
        log = `═══════════════════════════════════════\n`;
        log += `📧 HASIL OSINT EMAIL\n`;
        log += `═══════════════════════════════════════\n\n`;
        log += `📧 EMAIL: ${email}\n`;
        log += `${logo} PROVIDER: ${provider}\n`;
        log += `📏 PANJANG EMAIL: ${email.length} karakter\n`;
        if (email.includes("+")) {
            log += `🏷️ PLUS ALIAS: Terdeteksi\n`;
        }
        log += `👤 USERNAME: ${username}\n`;
        log += `📅 ESTIMASI DIBUAT: Tahun ${year} (simulasi)\n`;
        log += `🌐 DOMAIN: ${domain.includes("gmail") || domain.includes("yahoo") || domain.includes("outlook") ? "Publik (Umum)" : "Private/Custom"}\n`;
        log += `\n═══════════════════════════════════════\n`;
        log += `📱 TERDAFTAR DI PLATFORM:\n`;
        if (registered.length > 0) {
            registered.forEach(p => { log += `   ✅ ${p}\n`; });
        } else {
            log += `   ❌ Tidak terdeteksi\n`;
        }
        log += `\n═══════════════════════════════════════\n`;
        log += `🔒 STATUS KEAMANAN:\n`;
        log += `   ${Math.random() > 0.7 ? "⚠️ Pernah terdeteksi di breach database" : "✅ Aman, tidak terdeteksi di breach database"}\n`;
        log += `\n═══════════════════════════════════════\n`;
        log += `⚠️ Catatan: Data ini hanya simulasi demo.\n`;
        log += `═══════════════════════════════════════\n`;
    }
}
            else if(mode === 'getcontact') {
                tagArea.style.display = 'block';
                const tags = getStoredTags(phone);
                log += `LIST TAGS DATABASE:\n`;
                tags.forEach((t, i) => { log += `${i+1}. ${t}\n`; });
            }
            else {
                log += `WA_STATUS: REGISTERED\nTYPE     : ${phone.length > 12 ? 'BUSINESS' : 'REGULAR'}\nVERIFIED : ${phone.length > 12 ? 'YES' : 'NO'}\nLAST_ACT : ONLINE`;
            }

// ========== MUSIK BACKGROUND ==========
let isPlaying = false;
let audio = new Audio('Smile_spotdown.org.mp3');
audio.loop = true;

function toggleMusic() {
    if (isPlaying) {
        audio.pause();
        isPlaying = false;
        document.getElementById('musicBtn').innerHTML = '🎵 PLAY';
    } else {
        audio.play();
        isPlaying = true;
        document.getElementById('musicBtn').innerHTML = '🔇 STOP';
    }
}

            result.innerText = log;
        }, 5000);
    }
</script>
</body>
</html>
